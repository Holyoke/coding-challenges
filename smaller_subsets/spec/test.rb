require "byebug"

byebug

[1,2,3]

smaller_set = [1,2]

smaller_subsets = subsets([1,2])

bigger_subsets = [] 
smaller_subsets.each do |set|
	bigger_subsets << sets + [array.last]
end

smaller_subetts + bigger_subsets
end