(function () {
  // Make a namespace `Reverse Words`.

  if (typeof ReverseWords === "undefined"){
  	window.ReverseWords = {};
  }

  ReverseWords.swap = function (str, i, j) {
    console.log(str);
  };



})();
